import { types } from './types';

const state = {
    seeds: {
        type: {
            [types.standard]: 1,
        },
    },
    plants: {
        type: {
            [types.standard]: 0,
        },
    },
};

export { state };
